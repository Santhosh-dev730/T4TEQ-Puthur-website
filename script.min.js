// Logo Slider Animation
const slides = document.querySelectorAll(".logo-slide");
let showingSecond = false;

function animateLogos() {
    slides.forEach((slide) => {
        slide.style.transform = showingSecond ? "translateY(0)" : "translateY(-100%)";
    });
    showingSecond = !showingSecond;
}

if (slides.length > 0) {
    setTimeout(() => {
        animateLogos();
        setInterval(animateLogos, 4000);
    }, 3000);
}

// Particles JS Init
if (typeof particlesJS !== 'undefined') {
    particlesJS("particles-js", {
        "particles": {
            "number": { "value": 80, "density": { "enable": true, "value_area": 800 } },
            "color": { "value": "#a855f7" },
            "shape": { "type": "circle" },
            "opacity": { "value": 0.5 },
            "size": { "value": 3, "random": true },
            "line_linked": { "enable": false },
            "move": { "enable": true, "speed": 6, "direction": "none", "random": false, "straight": false, "out_mode": "out", "bounce": false }
        },
        "interactivity": {
            "detect_on": "canvas",
            "events": { "onhover": { "enable": true, "mode": "grab" }, "onclick": { "enable": true, "mode": "push" }, "resize": true },
            "modes": { "grab": { "distance": 140, "line_linked": { "opacity": 1 } }, "push": { "particles_nb": 4 } }
        },
        "retina_detect": true
    });
}

// Scroll Animation Observer & General DOM Ready Logic
document.addEventListener('DOMContentLoaded', () => {
    // AOS Init
    if (typeof AOS !== 'undefined') {
        AOS.init({
            duration: 1000,
            once: false,
            mirror: true,
            anchorPlacement: 'top-bottom',
        });
    }

    // Dynamic Copyright Year
    const copyrightYear = document.getElementById('copyright-year');
    if (copyrightYear) {
        copyrightYear.textContent = new Date().getFullYear();
    }

    const observerOptions = {
        root: null,
        rootMargin: '0px',
        threshold: 0.1
    };

    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('active');
            }
        });
    }, observerOptions);

    const hiddenElements = document.querySelectorAll('.reveal');
    hiddenElements.forEach(el => {
        el.classList.add('transition-all', 'duration-1000', 'opacity-0', 'translate-y-10');
        observer.observe(el);
    });

    // Dynamic Navbar Blending Logic
    const sections = document.querySelectorAll('[data-nav-color]');
    const nav = document.querySelector('nav');
    if (nav) {
        const navObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const color = entry.target.getAttribute('data-nav-color');
                    nav.style.backgroundColor = color;
                    nav.style.transition = 'background-color 0.8s ease';
                }
            });
        }, { threshold: 0.2 });
        sections.forEach(section => navObserver.observe(section));
    }

    // Mobile Menu Toggle Logic
    const mobileMenuBtn = document.getElementById('mobile-menu-btn');
    const closeMenuBtn = document.getElementById('close-menu-btn');
    const mobileMenu = document.getElementById('mobile-menu');
    if (mobileMenu) {
        const mobileContent = mobileMenu.querySelector('div');
        const mobileLinks = document.querySelectorAll('.mobile-nav-link');

        const toggleMobileMenu = (show) => {
            if (show) {
                mobileMenu.classList.remove('hidden');
                mobileMenu.offsetHeight; // force reflow
                mobileMenu.classList.remove('opacity-0');
                mobileContent.classList.remove('translate-x-full');
                document.body.style.overflow = 'hidden';
            } else {
                mobileMenu.classList.add('opacity-0');
                mobileContent.classList.add('translate-x-full');
                document.body.style.overflow = '';
                setTimeout(() => mobileMenu.classList.add('hidden'), 300);
            }
        };

        mobileMenuBtn?.addEventListener('click', () => toggleMobileMenu(true));
        closeMenuBtn?.addEventListener('click', () => toggleMobileMenu(false));
        mobileMenu.addEventListener('click', (e) => {
            if (e.target === mobileMenu) toggleMobileMenu(false);
        });
        mobileLinks.forEach(link => {
            link.addEventListener('click', () => toggleMobileMenu(false));
        });
    }

    // Navbar Scroll State Fix
    const mainHeader = document.getElementById('main-header');
    const footer = document.querySelector('footer');
    const handleNavScroll = () => {
        if (window.scrollY > 50) {
            mainHeader?.classList.add('scrolled');
            nav?.classList.remove('bg-slate-100/90');
            nav?.classList.add('bg-white/95', 'shadow-lg');
        } else {
            mainHeader?.classList.remove('scrolled');
            nav?.classList.add('bg-slate-100/90');
            nav?.classList.remove('bg-white/95', 'shadow-lg');
        }

        if (footer && nav) {
            const footerTop = footer.getBoundingClientRect().top;
            if (footerTop < 100) {
                nav.style.transform = 'translateY(-100%)';
                nav.style.opacity = '0';
            } else {
                nav.style.transform = 'translateY(0)';
                nav.style.opacity = '1';
            }
        }
    };
    window.addEventListener('scroll', handleNavScroll);
    handleNavScroll();

    // Initial setup for stacking cards
    cacheCardMeta();
    updateAnimations();
});

// Gallery Slider Logic
let currentGallerySlideNew = 0;
const totalGallerySlidesNew = 5;

window.updateGalleryCarouselNew = () => {
    const galleryMainSliderNew = document.getElementById('galleryMainSliderNew');
    const galleryTabsNew = document.querySelectorAll('.gallery-tab-new');
    if (!galleryMainSliderNew) return;

    const firstSlide = galleryMainSliderNew.querySelector('.gallery-slide-new');
    if (!firstSlide) return;

    const gap = 25;
    const slideWidth = firstSlide.offsetWidth + gap;
    galleryMainSliderNew.style.transform = `translateX(-${currentGallerySlideNew * slideWidth}px)`;

    galleryTabsNew.forEach((tab, index) => {
        tab.classList.toggle('active', index === currentGallerySlideNew);
    });
};

window.goToGallerySlideNew = (index) => {
    currentGallerySlideNew = index;
    updateGalleryCarouselNew();
};

// FAQ Toggle Logic
window.toggleFAQ = (index) => {
    const contents = document.querySelectorAll('.faq-content');
    const icons = document.querySelectorAll('.faq-icon');
    contents.forEach((content, i) => {
        if (i === index) {
            if (content.style.maxHeight) {
                content.style.maxHeight = null;
                icons[i].style.transform = 'rotate(0deg)';
            } else {
                content.style.maxHeight = content.scrollHeight + "px";
                icons[i].style.transform = 'rotate(180deg)';
            }
        } else {
            content.style.maxHeight = null;
            icons[i].style.transform = 'rotate(0deg)';
        }
    });
};

// Typewriter Animation Logic
const typewriterElement = document.getElementById('typewriter-text');
const words = ["Programming", "Full Stack", "Analytics", "Designing"];
let wordIndex = 0;
let charIndex = 0;
let isDeleting = false;
let typeSpeed = 150;

const type = () => {
    if (!typewriterElement) return;
    const currentWord = words[wordIndex];
    if (isDeleting) {
        charIndex--;
        typewriterElement.textContent = currentWord.substring(0, charIndex);
        typeSpeed = 100;
    } else {
        charIndex++;
        typewriterElement.textContent = currentWord.substring(0, charIndex);
        typeSpeed = 150;
    }

    if (!isDeleting && charIndex === currentWord.length) {
        isDeleting = true;
        typeSpeed = 2000;
    } else if (isDeleting && charIndex === 0) {
        isDeleting = false;
        wordIndex = (wordIndex + 1) % words.length;
        typeSpeed = 500;
    }
    setTimeout(type, typeSpeed);
};
if (typewriterElement) setTimeout(type, 1000);

// Skill Cards Stacking Animation
const skillCards = document.querySelectorAll('.skill-card');
const STICKY_TOP = 100;
let cardMeta = [];
let isScrollingAnimations = false;

const cacheCardMeta = () => {
    const scrollY = window.pageYOffset;
    cardMeta = Array.from(skillCards).map(card => {
        const rect = card.getBoundingClientRect();
        return { top: rect.top + scrollY, height: card.offsetHeight };
    });
};

const updateAnimations = () => {
    if (window.innerWidth < 1024) {
        skillCards.forEach(card => {
            card.style.transform = '';
            card.style.opacity = '';
            card.querySelectorAll('p, ul, .lang-btn, [id^="sidebar-"]').forEach(el => el.style.opacity = '1');
        });
        return;
    }

    const scrollY = window.pageYOffset;
    skillCards.forEach((card, index) => {
        const nextMeta = cardMeta[index + 1];
        if (nextMeta) {
            const distance = nextMeta.top - scrollY - STICKY_TOP;
            const cardHeight = cardMeta[index].height;
            let progress = Math.max(0, Math.min(1, 1 - (distance / cardHeight)));
            if (distance <= 0) progress = 1;

            card.style.transform = `scale(${1 - (progress * 0.05)}) translateY(${-progress * 20}px)`;
            card.style.opacity = (1 - (progress * 0.5)).toString();

            const contentOpacity = Math.max(0, 1 - (progress * 2));
            card.querySelectorAll('p, ul, .lang-btn').forEach(el => el.style.opacity = contentOpacity.toString());

            const sidebar = card.querySelector('[id^="sidebar-"]');
            if (sidebar) {
                if (progress > 0.6) { // Increased threshold to prevent early collapse
                    sidebar.style.display = 'none';
                    sidebar.classList.add("hidden");
                } else if (progress <= 0.6 && !sidebar.dataset.isManuallyHidden) {
                    if (!sidebar.classList.contains('hidden')) sidebar.style.display = 'flex';
                }
            }
        } else {
            card.style.transform = '';
            card.style.opacity = '1';
        }
    });
    isScrollingAnimations = false;
};

window.addEventListener('scroll', () => {
    if (!isScrollingAnimations) {
        requestAnimationFrame(updateAnimations);
        isScrollingAnimations = true;
    }
}, { passive: true });
window.addEventListener('resize', cacheCardMeta);

// Map Switching Logic
const branchMaps = {
    puthur: {
        name: "Puthur Branch (HO)",
        addr: "#5, All Saints Complex, Puthur Main Road, Trichy",
        url: "https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3918.841578!2d78.682393!3d10.823617!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3baaf5099356391d%3A0x6a0f6a67f18b9cad!2sT4TEQ%20SOFTWARE%20SOLUTIONS!5e0!3m2!1sen!2sin!4v1739328000000!5m2!1sen!2sin",
        phone: "7540046220",
        color: "border-orange-500"
    },
    thillai: {
        name: "Thillai Nagar ",
        addr: "C35, 1st Floor, K.M Towers, 5th Cross, Thillai Nagar, Trichy",
        url: "https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3918.8!2d78.68!3d10.83!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3baaf50000000000%3A0x0!2sT4TEQ%20Corporate%20Office!5e0!3m2!1sen!2sin!4v1739329000000!5m2!1sen!2sin",
        phone: "7540086220",
        color: "border-blue-500"
    },
    kattur: {
        name: "Kattur ",
        addr: "#12, Navaladiyan Arcade, Oil Mill Bus Stop, Kattur, Trichy",
        url: "https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3919.0!2d78.75!3d10.81!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3baaf50000000001%3A0x0!2sT4TEQ%20Kattur%20Branch!5e0!3m2!1sen!2sin!4v1739330000000!5m2!1sen!2sin",
        phone: "7448546220",
        color: "border-emerald-500"
    }
};

window.switchBranch = (branchId) => {
    const data = branchMaps[branchId];
    if (!data) return;

    // Update Map
    document.getElementById('branch-map').src = data.url;
    document.getElementById('map-branch-name').innerText = data.name;
    document.getElementById('map-branch-addr').innerText = data.addr;

    // Update Contact Links
    const quickChat = document.getElementById('quick-chat-link');
    const directCall = document.getElementById('direct-call-link');
    const quickChatNum = document.getElementById('quick-chat-num');
    const directCallNum = document.getElementById('direct-call-num');

    // Formatting phone number for display (e.g. +91 91591 59139)
    const formattedPhone = `+91 ${data.phone.substring(0, 5)} ${data.phone.substring(5)}`;

    if (quickChat) quickChat.href = `https://wa.me/91${data.phone}`;
    if (directCall) directCall.href = `tel:+91${data.phone}`;
    if (quickChatNum) quickChatNum.innerText = formattedPhone;
    if (directCallNum) directCallNum.innerText = formattedPhone;

    // Update Card Highlighting
    document.querySelectorAll('.address-card').forEach(card => {
        const id = card.id.replace('card-', '');
        const branchColor = branchMaps[id]?.color || 'border-orange-500';

        if (card.id === `card-${branchId}`) {
            card.classList.add('active', branchColor);
            card.classList.remove('border-slate-100');
        } else {
            card.classList.remove('active', 'border-orange-500', 'border-blue-500', 'border-emerald-500');
            card.classList.add('border-slate-100');
        }
    });
};

// Testimonial & Counters
function updateTestimonialFade() {
    const cards = Array.from(document.querySelectorAll(".testimonial-card"));
    if (!cards.length) return;
    const scrollBottom = window.scrollY + window.innerHeight;
    const section = document.getElementById('testimonials');
    if (!section) return;
    const distanceToBottom = (window.scrollY + section.getBoundingClientRect().bottom) - scrollBottom;
    cards.forEach(c => c.style.opacity = 1);
    const fadeStartThreshold = 200;
    if (distanceToBottom < fadeStartThreshold) {
        const lastCards = cards.slice(-3);
        const progress = Math.min(1, (fadeStartThreshold - distanceToBottom) / fadeStartThreshold);
        lastCards.forEach((card, index) => {
            const delay = index * 0.15;
            let local = Math.max(0, Math.min(1, (progress - delay) / (1 - delay)));
            card.style.opacity = 1 - local * 0.85;
        });
    }
}
window.addEventListener("scroll", updateTestimonialFade, { passive: true });

function animateCounters() {
    document.querySelectorAll('.count').forEach(el => {
        const target = parseInt(el.dataset.target);
        const duration = 1600;
        const start = performance.now();
        function step(now) {
            const elapsed = now - start;
            const progress = Math.min(elapsed / duration, 1);
            const ease = 1 - Math.pow(1 - progress, 3);
            const value = Math.floor(ease * target);
            el.textContent = value >= 1000 ? (value / 1000).toFixed(0) + ',000' : value;
            if (progress < 1) requestAnimationFrame(step);
            else el.textContent = target >= 1000 ? target.toLocaleString() : target;
        }
        requestAnimationFrame(step);
    });
}

const statsSection = document.getElementById('stats');
if (statsSection) {
    new IntersectionObserver(entries => {
        if (entries[0].isIntersecting) {
            animateCounters();
        }
    }, { threshold: 0.3 }).observe(statsSection);
}

// Topics & Course Details
const topics = {
    prog: {
        java: {
            color: "#FFEDD5", logo: "assets/java.svg",
            desc: "Master the art of building scalable, secure, and high-performance enterprise applications with Java. Gain deep insights into architecting robust systems that drive modern business solutions through industrial best practices.",
            syllabus: "Basics • OOPs • Packages • Multi-threading • JDBC "
        },
        python: {
            color: "#FEF9C3", logo: "assets/python.svg",
            desc: "Explore the versatility of Python for everything from web development to data science and automation. Write clean, efficient code and innovate faster with one of the most popular and powerful languages today.",
            syllabus: "Basics • Modules • Collections • OOPs • DB Connectivity "
        },
        sql: {
            color: "#E0F2FE", logo: "assets/sql.svg",
            desc: "Master the essential skills for structuring, managing, and querying complex databases efficiently. Learn to turn raw information into actionable business insights with advanced SQL techniques and PL/SQL logic.",
            syllabus: "SQL Commands • Joins & Queries • PL/SQL • Procedures • Cursor "
        },
        dsa: {
            color: "#FEE2E2", logo: "assets/dsa.png",
            desc: "Develop a rock-solid foundation in problem-solving by mastering arrays, stacks, trees, and algorithms. This course is designed to sharpen your logic and prepare you for top-tier technical coding interviews.",
            syllabus: "Collections • Stack • Queue & List • Graphs & Trees • Algorithm"
        }
    },
    fs: {
        react: {
            color: "#DBEAFE", logo: "assets/react.svg",
            desc: "Build dynamic, high-performance user interfaces that captivate users with seamless interactions and modern design. Master the React ecosystem to create sophisticated web applications that stand out in today's digital world.",
            syllabus: "Components & Props • Event Handling • Hooks & Routing • Forms  • API Integration"
        },
        webdev: {
            color: "#FFEDD5", logo: "assets/web.webp",
            desc: "Launch your career in web development by mastering the core pillars of the modern web: HTML, CSS, and JavaScript. Learn to build responsive, beautiful layouts and interactive features using industry-standard tools and frameworks.",
            syllabus: "HTML & CSS • Javascript • DOM & BOM •  Bootstrap •  SEO & CRO •  Content Marketing & Google Ads "
        },
        spring: {
            color: "#DCFCE7", logo: "assets/spring_boot.svg",
            desc: "Architect powerful enterprise solutions with Spring boot's streamlined configuration and powerful ecosystem. Learn to build scalable, production-ready Java backend systems that meet the highest industrial standards.",
            syllabus: "Core Concepts • Thymeleaf  • Security • Microservices • API Integration  "
        },
        django: {
            color: "#F1F5F9", logo: "assets/django.svg",
            desc: "Rapidly build secure and maintainable web applications using the powerful Django framework. Learn the 'batteries-included' philosophy to develop complex backend systems with clean, efficient Python code.",
            syllabus: "Core Concepts • Templates & Static files • Models and Forms • Authentication • API Integration "
        },
        node: {
            color: "#FEF9C3", logo: "assets/node.svg",
            desc: "Build high-speed, real-time backend applications using the event-driven architecture of Node.js. Master server-side JavaScript to create scalable, modern web services that can handle thousands of concurrent connections.",
            syllabus: "Core Concepts • NPM & Server • Express • DB Connectivity • API Integration"
        }
    },
    da: {
        dapython: {
            color: "#DBEAFE", logo: "assets/python.svg",
            desc: "Leverage the power of Python to clean, process, and analyze complex datasets with ease. Build a strong foundation in data automation, visualization, and statistical analysis to drive data-driven decision making.",
            syllabus: "Basic Python •  NumPy & Pandas • Data Visualization • EDA • Statistics"
        },
        excel: {
            color: "#E0F2FE", logo: "assets/excel.svg",
            desc: "Master data organization and advanced analysis using the full power of Microsoft Excel. Learn to build complex models, automate tasks with VBA, and create professional reports that drive business intelligence.",
            syllabus: "Data Visualization • Analysis Tools • Data Manpulation • Data Validation  • VBA"
        },
        powerbi: {
            color: "#DCFCE7", logo: "assets/powerbi.svg",
            desc: "Transform raw data into beautiful, interactive dashboards that tell a compelling story. Master Power BI to drive smarter business decisions through sophisticated visual analytics and real-time data monitoring.",
            syllabus: "Visualization • DAX & Data Models • ETL Tools • Interactive Dashboards • Fabric"
        },
        aiml: {
            color: "#EEF2FF", logo: "assets/ai.svg",
            desc: "Dive into the future of technology by exploring intelligent systems that simulate human decision-making. Learn to build and deploy predictive models that automatically learn and improve from large-scale data patterns.",
            syllabus: "Basics • ML Libraries • Supervised & Unsupervised  • Reinforcment •  NLP "
        }
    },
    design: {
        figma: {
            color: "#FFEDD5", logo: "assets/figma.svg",
            desc: "Transform your creative ideas into interactive, professional designs using Figma. Master the art of collaborative UI/UX design to build intuitive and beautiful user experiences for web and mobile platforms.",
            syllabus: "Fundamentals • Prototypes • Layout & Positioning • Components • Wireframing"
        },
        photoshop: {
            color: "#FEF9C3", logo: "assets/illustrator.svg",
            desc: "Create professional graphics and stunning visual content using industry-standard Adobe tools. Master Photoshop and Illustrator to bring your digital creativity to life with high-impact designs and imagery.",
            syllabus: "Image Editing • Retouching • Layers & Masks • Design Principles • Graphic Designing"
        },
        flutter: {
            color: "#E0F2FE", logo: "assets/flutter.svg",
            desc: "Build beautiful, high-performance cross-platform mobile applications from a single codebase using Flutter. Create expressive and native-like experiences for iOS and Android with fast development cycles.",
            syllabus: "Overview of Dart • OOPs • State Management • Routing • Widgets & Navigation"
        },
        rn: {
            color: "#DBEAFE", logo: "assets/react_native.svg",
            desc: "Develop native mobile applications with the power of React and JavaScript. Build high-performance, responsive apps for multiple platforms while leveraging the familiar React component-based architecture.",
            syllabus: "Overview of React Native • Hooks • State Management • Native Apps • API Integration"
        }
    }
};

function showTopics(section, lang, event) {
    const sidebar = document.getElementById("sidebar-" + section);
    const e = event || window.event;
    const clickedBtn = e ? (e.currentTarget || (e.target ? e.target.closest('.lang-btn') : null)) : null;

    if (clickedBtn && clickedBtn.dataset.active === "true") {
        const content = sidebar.querySelector('.flex.flex-col.w-full');
        if (content) {
            content.style.opacity = '0';
            content.style.transform = 'translateY(-10px)';
            content.style.transition = 'all 0.4s ease';
        }
        setTimeout(() => {
            sidebar.classList.add("hidden");
            sidebar.classList.remove("flex");
            sidebar.style.display = 'none';
            sidebar.dataset.isManuallyHidden = "true";
            clickedBtn.style.transform = "scale(1)";
            clickedBtn.style.boxShadow = "";
            delete clickedBtn.dataset.active;
            window.dispatchEvent(new Event('resize'));
        }, 400);
        return;
    }

    document.querySelectorAll(`[onclick*="${section}"]`).forEach(btn => {
        btn.style.transform = "scale(1)";
        btn.style.boxShadow = "";
        delete btn.dataset.active;
    });

    const data = topics[section][lang];
    if (clickedBtn) {
        clickedBtn.style.transform = "scale(1.05)";
        clickedBtn.style.boxShadow = `0 0 0 4px ${data.color}`;
        clickedBtn.dataset.active = "true";
    }

    sidebar.classList.remove("hidden");
    sidebar.classList.add("flex");
    sidebar.style.display = 'flex';
    delete sidebar.dataset.isManuallyHidden;

    const list = document.getElementById("list-" + section);
    const syllabusItems = data.syllabus.split('•').map(s => s.trim());
    const syllabusCards = syllabusItems.map((item, i) => `
        <div class="px-3 py-1 bg-white border border-gray-100/10 rounded-full text-l font-semibold text-gray-700 shadow-sm hover:shadow-md transition-all cursor-default" style="opacity:0; transform: translateY(15px); animation: courseSlideUp 0.4s ease forwards; animation-delay: ${i * 0.08}s;">
            ${item}
        </div>
    `).join('');

    // MODIFICATION: Removed Java underline and reduced margin
    const descClass = 'text-slate-600 text-md leading-relaxed line-clamp-2';

    const contentHtml = `
        <style>
            @keyframes courseSlideUp { to { opacity: 1; transform: translateY(0); } }
        </style>
        <div class="flex flex-col w-full gap-2">
            <div class="flex items-start justify-between gap-4">
                <div class="w-full lg:w-3/4" style="opacity:0; transform: translateY(20px); animation: courseSlideUp 0.6s ease forwards; animation-delay: 0.1s;">
                    <h4 class="text-2xl font-bold mb-1 uppercase tracking-tight text-slate-900">${lang.replace('rn', 'React Native').replace('ui', 'UI/UX').replace('dapython', 'Data Analytics python').replace('aiml', 'AI & ML').replace('webdev', 'Web Development with Digital Marketing').replace('spring', 'Spring boot')}</h4>
                    <p class="${descClass}">
                        ${data.desc}
                    </p>
                </div>
                <!-- MODIFICATION: Show logo on mobile as well -->
                <div class="flex w-1/4 justify-end shrink-0" style="opacity:0; transform: translateY(20px); animation: courseSlideUp 0.6s ease forwards; animation-delay: 0.2s;">
                    <div class="bg-white p-2 rounded-2xl shadow-sm border border-slate-100">
                        <img src="${data.logo}" alt="${lang}" class="w-12 h-12 sm:w-20 sm:h-20 object-contain">
                    </div>
                </div>
            </div>

            <!-- Horizontal Line -->
             <div class="h-[2px] w-full bg-white/50 rounded"></div>

            <div class="w-full">
                <span class="text-[12px] font-black uppercase tracking-widest text-slate-500 block mb-2">Syllabus Highlights</span>
                <div class="flex flex-wrap gap-2">
                    ${syllabusCards}
                </div>
            </div>

            <div class="flex flex-col sm:flex-row items-center justify-between gap-6" style="opacity:0; transform: translateY(20px); animation: courseSlideUp 0.6s ease forwards; animation-delay: 0.4s;">
                <div class="flex flex-wrap items-center justify-center sm:justify-start gap-4 sm:gap-8 text-slate-600">
                    <div class="flex items-center gap-2.5">
                        <i class="fas fa-circle-check text-slate-700"></i>
                        <span class="text-sm sm:text-sm font-normal">1 Certificate</span>
                    </div>
                    <div class="flex items-center gap-2.5">
                        <i class="fas fa-briefcase text-slate-700"></i>
                        <span class="text-sm sm:text-sm font-normal">Real-time projects</span>
                    </div>
                    <div class="flex items-center gap-2.5">
                        <i class="fas fa-address-card text-slate-700"></i>
                        <span class="text-sm sm:text-sm font-normal">Live Support</span>
                    </div>
                     <div class="flex items-center gap-2.5">
                        <i class="fas fa-user-graduate text-slate-700"></i>
                        <span class="text-sm sm:text-sm font-normal">Internship</span>
                    </div>
                </div>
                <div class="w-full sm:w-auto">
                    <button type="button" onclick="sendToWhatsApp('${section.replace('prog', 'Programming').replace('fs', 'Full Stack').replace('da', 'Data Analytics').replace('design', 'Designing').replace('webdev', 'Web Development').replace('dsa', 'Data Structures & Algorithms').replace('powerbi', 'Power BI').replace('d')}')"
                        class="w-full sm:w-auto px-10 py-3 text-white bg-blue-600 rounded-xl font-bold text-lg hover:bg-blue-700 shadow-xl hover:shadow-blue-200 transition-all transform hover:-translate-y-1 duration-200">
                        Enroll Now
                    </button>
                </div>
            </div>
        </div>
    `;

    list.className = "w-full transition-all duration-500 ease-in-out";
    list.style.opacity = '1';
    list.style.transform = 'translateY(0)';
    list.innerHTML = contentHtml;
    window.currentSelectedCourse = lang;
    cacheCardMeta();
    updateAnimations();
    setTimeout(() => list.scrollIntoView({ behavior: 'smooth', block: 'nearest' }), 100);
}

function sendToWhatsApp(courseName) {
    var course = courseName || window.currentSelectedCourse || "General Inquiry";
    var displayCourse = course;
    if (course === "prog") displayCourse = "Programming";
    else if (course === "fs") displayCourse = "Full Stack";
    else if (course === "da") displayCourse = "Data Analytics";
    else if (course === "design") displayCourse = "Designing";

    var phoneNumber = "917092406372";
    var message = "Hello! I want enquiry details.\n\n" + "Course: " + displayCourse;
    var url = "https://wa.me/" + phoneNumber + "?text=" + encodeURIComponent(message);
    window.open(url, "_blank");
}

// Celebration Sequence Logic

function openCelebrationModal() {
    const modal = document.getElementById("celebrationModal");
    if (modal) {
        modal.classList.add("show");
        launchConfetti(0.6);

        // Auto close after 6 seconds for poster visibility
        setTimeout(closeCelebrationModal, 6000);
    }
}

function closeCelebrationModal() {
    const modal = document.getElementById("celebrationModal");
    if (modal) {
        modal.classList.remove("show");
    }
}

function startCelebrationSequence() {
    const heroCelebration = document.getElementById("hero-celebration");

    // Step 1: Show Hero Popup
    setTimeout(() => {
        if (heroCelebration) {
            heroCelebration.classList.add("active");
            confetti({
                particleCount: 150,
                spread: 70,
                origin: { y: 0.5 }
            });

            // Step 2: Hide Hero Popup and Show Modal
            setTimeout(() => {
                heroCelebration.classList.remove("active");
                setTimeout(openCelebrationModal, 500);
            }, 2500);
        } else {
            // Fallback
            openCelebrationModal();
        }
    }, 800);
}

// Global exposure
window.closeCelebration = closeCelebrationModal;

// Initialize on load
window.addEventListener('load', () => {
    startCelebrationSequence();
});
